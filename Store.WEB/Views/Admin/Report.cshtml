@model IEnumerable<IGrouping<String, Store.WEB.Models.ReportViewModels.ReportIndexModel>>
@*@model IEnumerable<Store.WEB.Models.ReportViewModels.ReportIndexModel>*@

@{
    ViewBag.Title = "Report";
}

<h2>Отчет</h2>

<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.User)*@
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Good)*@
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.DateSale)*@
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model)*@
        </th>

    </tr>

    @foreach (var item in Model)
    {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Key)
            </td>

            <td>
                <table>
                    @foreach (var i in item)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => i.Good)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => i.DateSale)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => i.Cost)
                            </td>
                            <td></td>
                        </tr>
                    }
                </table>

            </td>

        </tr>
    }

</table>
